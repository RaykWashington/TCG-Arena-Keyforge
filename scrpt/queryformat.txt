map({
id: .houses.0.id,
face:{
  front:{
    name: .card_title,
    type: .card_type,
    cost: 0,
    image: .houses.0.normal
    }
  },
  name: .card_title,
  type: .card_type,
  house: .houses.0.house,
  cost: 0,
  expansion: .set,
  rarity: .rarity,
  isToken :  if(.card_type == "Token Creature", true , false)
  })|keyBy(string(.id))